# AGENTS.md

**The governing document for this repository is [`CLAUDE.md`](CLAUDE.md). Read that.**

This file used to be a full copy of it — 2,298 lines, headed `# CLAUDE.md`, declaring itself
authoritative as of 2026-08-20. It was a *pre-compaction* copy, so by 2026-08-27 the two disagreed:
`CLAUDE.md` carried `D63`–`D68` and this file did not, while both claimed to govern.

That is the failure mode `D54a` names and this project keeps meeting: **two artifacts required to
agree, with nothing checking that they did**. The cheapest permanent fix is to stop having two.
Nothing is lost — `CLAUDE.md` is the compacted successor, `docs/DIVERGENCE_REGISTER.md` carries the
long-form evidence the compaction moved out of §14, and the deleted text is in git history at
`2dc8706` and earlier.

| Looking for | Read |
|---|---|
| Project law — constraints, RQs, protocol, metrics, layout | `CLAUDE.md` |
| Why any rule is what it is, with its defect ID | `docs/DIVERGENCE_REGISTER.md` |
| Commands, stages, schemas, expected numbers | `USAGE.md` |
| Per-origin / per-block window accounting | `docs/ORIGIN_WINDOW_BUDGET.md` |
| Manuscript writing posture | `paper/CLAUDE.md` |

Registered as `D69`.
